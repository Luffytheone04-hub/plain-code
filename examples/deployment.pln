// Plain v1.0.1 — runtime dependency and deployment example
//
// `plain build` and `plain run` detect express from `web app` and install it
// when it is missing. `plain start` uses the entry in plain.json.

web app

route "/"
    reply "Plain is ready to deploy!"
done

start 3000