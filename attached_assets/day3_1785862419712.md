Implement:

make

give

Function calls

Example:

make greet()
    show "Hello"
done

greet()

Example:

make add(a, b)
    give a + b
done

show add(5, 7)

Generate valid JavaScript.

Keep compatibility with previous versions.

Update parser, AST and generator.

Add tests.